# Synthetic AoS 4 Export Fixture

This fixture mirrors the CSV file names and pipe-delimited structure expected by
the app, but all names, text, links, IDs, and source metadata are fictional.

It is used only to demonstrate the card composer before a user loads their own
local export files in the browser.
